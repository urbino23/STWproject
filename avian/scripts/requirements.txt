# Image pipeline dependencies for avian/scripts/.
# import_plates.py needs Pillow (downscaling). pregen.py needs only Pillow
# (for reference downscaling) and degrades gracefully without it. cutout.py
# needs rembg + onnxruntime for the BiRefNet matting model. verify.py is
# standard-library only.
Pillow>=10.0
rembg>=2.0.76
onnxruntime>=1.16
